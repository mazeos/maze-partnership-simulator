services:
  maze-partnership-simulator-dev:
    image: nginx:alpine
    restart: unless-stopped
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - traefik-public
    labels:
      - traefik.enable=true
      - traefik.http.routers.maze-partnership-simulator-dev.rule=Host(`partnership-test.mazefunnels.io`)
      - traefik.http.routers.maze-partnership-simulator-dev.entrypoints=websecure
      - traefik.http.routers.maze-partnership-simulator-dev.tls.certresolver=letsencrypt
      - traefik.http.services.maze-partnership-simulator-dev.loadbalancer.server.port=80

networks:
  traefik-public:
    external: true
