# Archivos basura del sistema (macOS)
.DS_Store

# Carpetas de dependencias (por si más adelante el proyecto crece)
node_modules/

# Variables de entorno con secretos (NUNCA se suben)
.env
.env.local
