services:
  maze-partnership-simulator:
    image: nginx:alpine
    restart: unless-stopped
    volumes:
      # Servimos SOLO el index.html (no exponemos .git, README, etc.)
      - ./index.html:/usr/share/nginx/html/index.html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - traefik-public
    labels:
      - traefik.enable=true
      - traefik.http.routers.maze-partnership-simulator.rule=Host(`partnership.mazefunnels.io`)
      - traefik.http.routers.maze-partnership-simulator.entrypoints=websecure
      - traefik.http.routers.maze-partnership-simulator.tls.certresolver=letsencrypt
      - traefik.http.services.maze-partnership-simulator.loadbalancer.server.port=80

networks:
  traefik-public:
    external: true
